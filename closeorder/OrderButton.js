button.push({
  title: 'Close Order', cssClass: 'edit-button',
  onClick: () => {
    var dlg = new TrackLog.OrderDialog();
    this.load(<TrackLog.OrderRow>{
      ShippedDate: Q.formatDate(new Date(), 'yyyy-MM-dd'),
    }
  });
return buttons;

limit_ordered_info_ask = exchange[index[ask_exchange]].create_order(
  symbol = TARGET_CURRENCY,
  type = "limit",
  side = "buy",
  amount = set_ask_volume,
  price = best_ask_price
)





